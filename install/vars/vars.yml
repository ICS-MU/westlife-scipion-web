---

be_src_dir: "../cfy-wrapper/"

cfy_wrapper_dir: "/opt/cfy-wrapper/"
template_dir: "{{ cfy_wrapper_dir }}template/"
deployments_dir: "{{ cfy_wrapper_dir }}deployments/"

log_dir: "{{ cfy_wrapper_dir }}log/"
scripts_dir: "{{ cfy_wrapper_dir }}backend/"

be_user: "be-user"
be_group: "be-group"
app_src_dir: "../app/"
app_dir: "/var/www/scipion-api/"
api_file: "api.py"
fe_user: "www-data"
